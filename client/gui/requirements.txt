# Datang Reader GUI Client - Python Dependencies
# This file contains dependencies for the HOST GUI client (not for Docker container)

# HTTP API Client (for communicating with Docker container)
requests>=2.28.0

# GUI Framework (PyQt5 for modern UI)
PyQt5>=5.15.0

# Optional: For system tray icon support
pystray>=0.19.0
Pillow>=9.0.0

# Note: This version uses HID keyboard-based RFID readers
# No serial port communication libraries needed
