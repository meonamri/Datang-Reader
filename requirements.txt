# Datang Reader Linux Service - Python Dependencies

# HTTP API Client
requests>=2.28.0

# HTTP Server (for Docker deployment mode)
Flask>=2.0.0

# GUI Framework (PyQt5 for modern UI)
PyQt5>=5.15.0

# Database for offline queue
# (sqlite3 is included in Python standard library, no need to install)

# Optional: For system tray icon support
pystray>=0.19.0
Pillow>=9.0.0

# Development/Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Note: This version uses HID keyboard-based RFID readers
# No serial port communication libraries needed
